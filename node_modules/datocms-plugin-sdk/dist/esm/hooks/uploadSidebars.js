import { isArray, isNullish, isNumber, isRecord, isString, } from '../guardUtils.js';
export function isUploadSidebar(value) {
    return (isRecord(value) &&
        isString(value.id) &&
        isString(value.label) &&
        (isNullish(value.parameters) || isRecord(value.parameters)) &&
        (isNullish(value.rank) || isNumber(value.rank)) &&
        (isNullish(value.preferredWidth) || isNumber(value.preferredWidth)));
}
export function isReturnTypeOfUploadSidebarsHook(value) {
    return isArray(value, isUploadSidebar);
}
//# sourceMappingURL=uploadSidebars.js.map