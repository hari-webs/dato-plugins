import { containedRenderModeBootstrapper } from '../utils';
export var renderItemFormOutletBootstrapper = containedRenderModeBootstrapper('renderItemFormOutlet', function (configuration, ctx) {
    if (!configuration.renderItemFormOutlet) {
        return;
    }
    configuration.renderItemFormOutlet(ctx.itemFormOutletId, ctx);
});
//# sourceMappingURL=renderItemFormOutlet.js.map