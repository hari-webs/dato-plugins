import { containedRenderModeBootstrapper } from '../utils';
export var renderFieldExtensionBootstrapper = containedRenderModeBootstrapper('renderFieldExtension', function (configuration, ctx) {
    if (!configuration.renderFieldExtension) {
        return;
    }
    configuration.renderFieldExtension(ctx.fieldExtensionId, ctx);
});
//# sourceMappingURL=renderFieldExtension.js.map