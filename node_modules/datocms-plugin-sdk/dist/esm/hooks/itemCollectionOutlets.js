import { isArray, isNullish, isNumber, isRecord, isString, } from '../guardUtils.js';
export function isItemCollectionOutlet(value) {
    return (isRecord(value) &&
        isString(value.id) &&
        (isNullish(value.rank) || isNumber(value.rank)) &&
        (isNullish(value.initialHeight) || isNumber(value.initialHeight)));
}
export function isReturnTypeOfItemCollectionOutletsHook(value) {
    return isArray(value, isItemCollectionOutlet);
}
//# sourceMappingURL=itemCollectionOutlets.js.map