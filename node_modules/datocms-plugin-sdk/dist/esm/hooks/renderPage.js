import { fullScreenRenderModeBootstrapper } from '../utils';
export var renderPageBootstrapper = fullScreenRenderModeBootstrapper('renderPage', function (configuration, ctx) {
    if (!configuration.renderPage) {
        return;
    }
    configuration.renderPage(ctx.pageId, ctx);
});
//# sourceMappingURL=renderPage.js.map