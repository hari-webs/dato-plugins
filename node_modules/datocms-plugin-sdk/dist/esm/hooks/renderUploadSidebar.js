import { fullScreenRenderModeBootstrapper } from '../utils';
export var renderUploadSidebarBootstrapper = fullScreenRenderModeBootstrapper('renderUploadSidebar', function (configuration, ctx) {
    if (!configuration.renderUploadSidebar) {
        return;
    }
    configuration.renderUploadSidebar(ctx.sidebarId, ctx);
});
//# sourceMappingURL=renderUploadSidebar.js.map